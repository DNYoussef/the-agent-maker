# Agent Forge V2 - Development Dependencies

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0  # Parallel testing
pytest-timeout>=2.2.0
pytest-mock>=3.15.1
pytest-asyncio==0.23.8  # Required: phase3 data_generator tests are genuinely async

# Code Quality
# isort/flake8 pinned (not floating >=) so CI matches the formatting/lint results
# verified locally; a floating isort picked a version that reordered one __init__.
black==26.5.1  # Auto-formatter - pinned
isort==5.13.2  # Import sorter - pinned
flake8==7.3.0  # Linter - pinned
pylint>=3.0.0  # Advanced linter
mypy>=1.8.0  # Type checker

# Pre-commit hooks
pre-commit>=3.6.0

# Type stubs
types-PyYAML>=6.0.0
types-requests>=2.31.0

# Documentation
sphinx>=8.1.3
sphinx-rtd-theme>=2.0.0

# Development tools
ipython>=8.39.0
jupyter>=1.1.1

# CI/CD
coverage>=7.4.0
tox>=4.11.0

# Async HTTP (for Phase 3 data generation)
aiohttp>=3.9.0
